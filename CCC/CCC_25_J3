'''
1
AbC3c2Cd9

3
Ahkiy-6ebvXCV1
393hhhUHkbs5gh6QpS-9-8
PL12N-2G1234Duytrty8-86tyaYySsDdEe
'''


codes_num = int(input())
code_list = []
for i in range(codes_num):
    code = input()
    code_list.append(code)

for cade in code_list:
    caps = ''
    number = 0
    current = ''
    neg = False
    for i in range(len(cade)):
        if cade[i].isupper():
            caps += cade[i]
        if cade[i].isdigit() and not cade[i].isalpha():
            current += cade[i]
            print("current: " + str(current))
        if cade[i] == '-':
            neg = True
        elif neg and cade[i].isdigit():
            current += cade[i]
        else:
            neg = False
        if current != '':
            number += int(current)
            current = ''
            print("number: " + str(number))
    if current != '':
        number += int(current)
        current = ''
        print("number: " + str(number))
    print(caps + str(number))